
  # Automatic Timetable Scheduler UI

  This is a code bundle for Automatic Timetable Scheduler UI. The original project is available at https://www.figma.com/design/Iz9E3k6HQ5vVcNKAC8lkbR/Automatic-Timetable-Scheduler-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  